
    1️⃣ What is the difference between var, let, and const?

    Ans:
    *var = Function Scoped , can be re-declared and updated.

    ** let = Block scoped , can be updated but not re-declared.

    *** const = Block scoped , cannot br updated or re-declared .const very Useful for code.


   2️⃣ What is the spread operator (...)?

    Ans:
    **The spread operator is used to copy or expand arrays and objects into individual elements.

  3️⃣ What is the difference between map(), filter(), and forEach()?

  Ans :
  ***map(): Returns a new array by transforming each element.

***filter(): Returns a new array with elements that match a condition.

***forEach(): Executes a function for each element (does not return a new array).

4️⃣ What is an arrow function?
Ans:
***An arrow function is a short and modern way to write functions in JavaScript.

5️⃣ What are template literals?
Ans:
***Template literals use backticks (`) to create strings and allow embedding variables and expressions easily.


